targets:
  $default:
    builders:
      hive_generator:hive_generator:
        generate_for:
          - lib/core/models/*.dart
        options:
          build_extensions:
            ".dart": [".g.dart"]

